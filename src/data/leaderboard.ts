import type { LeaderboardPeriod, LeaderboardRow, LeaderboardTab } from "@/types";

/** Mock leaderboard rows — populated in Phase 4 */
export function getLeaderboard(
  tab: LeaderboardTab,
  period: LeaderboardPeriod,
): LeaderboardRow[] {
  void tab;
  void period;
  return [];
}
